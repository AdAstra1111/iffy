import React from "react";

const DeliverWorkspace: React.FC = () => {
  return (
    <div className="flex flex-col items-center justify-center min-h-[60vh] p-8 text-center">
      <h1 className="text-3xl font-bold mb-4">Deliver workspace — coming soon</h1>
      <p className="text-muted-foreground max-w-lg text-lg">
        Export your project in every format — script PDFs, FDX, ICS, XLS, and share packs.
      </p>
    </div>
  );
};

export default DeliverWorkspace;