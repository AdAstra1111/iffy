# SAT-1 DELTA REPORT — Stabilization Sprint

**Date:** 2026-05-31  
**Previous:** SAT-1 baseline (4 P2, 3 P3 issues)  
**After:** Stabilization sprint (5 edge functions patched, 1 DB migration)

---

## RESOLVED

| Issue | Severity | Status | Fix |
|-------|----------|--------|-----|
| P2.1 — Visual DNA not auto-generated from atoms | P2 | ✅ FIXED | Added `generate_from_atoms` mode to `generate-visual-dna-from-canon`. Queries `narrative_entities` for character names (reliable `canonical_name`), then falls back to `atoms` table. New `processCharacterList` helper eliminates duplicated processing loops. |
| P2.2 — Scene/entity errors are noisy | P2 | ✅ FIXED | Replaced `throw new Error()` with `{status: "skipped", reason: "...", stage: "..."}` in nel-orchestrator. Errors are now structured non-fatal skips. |
| P2.3 — vpb-export returns 404 | P2 | ✅ FIXED | Changed 404 → 200 with `{exists: false, projectId, versionNumber: null, markdown: ""}`. Frontend-safe. |
| P3.1 — Missing projectId returns 500 | P3 | ✅ FIXED | vpb-export and vpb-assembly-engine now return 400 with structured error message. |
| P3.2 — Malformed UUID returns 500 | P3 | ✅ FIXED | UUID regex validation added before DB queries. Returns 400. |

## PARTIALLY RESOLVED

| Issue | Severity | Status | Remaining |
|-------|----------|--------|-----------|
| P2.4 — Cast/Wardrobe/Hero Frames always 0 | P2 | ⚠️ PARTIAL | **Wardrobe: FIXED** — `character_wardrobe_profiles` table had missing columns + no unique constraint. Migration applied. Now populates correctly. **Cast: OPEN** — requires `ai_actors` binding pipeline. **Hero Frames: OPEN** — requires governance clearance + `generate-hero-frames` invocation. |

## SAT-1 DELTA RESULTS

| Project | Before | After | Delta |
|---------|--------|-------|-------|
| Concrete Angels | v4, 6,282 chars, 10 pop sec | v4, 6,282 chars | ✅ Stable |
| YETI (newest) | v1, 2,818 chars, 9 pop sec | v2, 2,818 chars | ✅ Structured errors |
| The Long Dusk | v1, 2,521 chars, 8 pop sec (no wardrobe) | v2, 2,558 chars, **Wardrobe(1)** | ✅ New content |
| The Encore | v1, 2,578 chars, 8 pop sec (no wardrobe) | v4, 2,649 chars, **Wardrobe(4)** | ✅ New content |
| [TEST] Vert Drama | v1, 5,401 chars, 10 pop sec (no vdna) | v2, 5,755 chars, **Wdrobe(2), DNA(4)** | ✅ New content |

## REMAINING OPEN ISSUES

- **Cast binding**: Not an automated pipeline step. Requires manual actor selection or auto-cast.
- **Hero frame generation**: Blocked on cast binding + governance clearance. Cannot auto-trigger.
- **No P0 or P1 issues** found in any run.

## FILES CHANGED

| File | Change |
|------|--------|
| `supabase/functions/nel-orchestrator/index.ts` | 4 throw → structured skip; visual DNA mode → `generate_from_atoms` |
| `supabase/functions/generate-visual-dna-from-canon/index.ts` | New `generate_from_atoms` mode; `processCharacterList` helper; `narrative_entities` query |
| `supabase/functions/vpb-export/index.ts` | 404 → 200 + `{exists: false}`; UUID validation; structured error responses |
| `supabase/functions/vpb-assembly-engine/index.ts` | UUID validation; 400 for malformed input |
| `supabase/migrations/20260531000002_fix_wardrobe_profile_columns.sql` | Added missing columns + unique constraint to `character_wardrobe_profiles` |
| `memory/sat-1-report-2026-05-31.md` | Baseline SAT-1 report |

## VALIDATION

All changes deployed to production Supabase (`hdfderbphdobomkdjypc`). Verified by re-running SAT-1 across all 5 projects.

**Definition of Done achieved:** A production-ready project (Concrete Angels) moves from Approved Corpus → NEL → Visual DNA → VPB without manual intervention. Cast and hero frames remain separate concerns.