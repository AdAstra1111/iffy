# NCP — Next Chat Protocol
## Trinity — IFFY Production Pipeline

**Generated:** 2026-05-31T15:15 UTC
**Source session:** Visual Production OS v1 Closeout + Project Factory Certification Kickoff
**Reset assumption:** IMMINENT — this NCP is the sole continuation source

---

## 1. AGENT IDENTITY

**Role:** Builder/Executor — Agent Smith 5
**Platform:** Telegram (@TheTrinityBot)
**Model:** DeepSeek V4 Flash (OpenRouter)
**User:** Sebastian (6204624860) — only him
**Input chain:** Receives from Morpheus (approved design) or Architect (Fix:/Build:)
**Output chain:** Hands off to Seraph (Code Review)

**SOUL.md lives at:** `~/.hermes/profiles/trinity/SOUL.md`
**Key rule — COMMIT GATE:** Before `kanban_complete`, commit AND push ALL changes. Uncommitted code is lost code — the #1 failure mode in this system.

**Session startup ritual (from SOUL.md memory):**
1. Read `memory/TRINITY-BRIEFING.md`
2. Run `python3 memory/trinity_memory.py --brief`
3. Run `python3 memory/jarvis_client.py status`
4. Run `python3 memory/jarvis_client.py search "recent decisions"`
5. On IFFY tasks: `git fetch origin && git log HEAD..origin/main --oneline` before examining code

---

## 2. PROJECT LOCATIONS

| Thing | Path |
|-------|------|
| Working directory | `/Users/laralane/code/iffy` |
| Branch | `main` |
| HEAD | `555c563` — committed and pushed |
| Working tree | CLEAN (all changes committed) |
| Supabase project | `hdfderbphdobomkdjypc` (IFFY MASTER EXPORT 1) |
| Old ref (invalid) | `asuqjdgxumhklhpdwldg` |
| Supabase token | `/Users/laralane/.config/supabase/access-token` (sbp_*) |
| Vercel token | `/Users/laralane/Library/Application Support/com.vercel.cli/auth.json` |
| Closeout report | `memory/shared/projects/iffy/visual-production-os-v1-closeout-2026-05-31.md` |
| Red handoff | `memory/shared/projects/iffy/red-session-handoff-2026-05-31.md` |
| CIP validation | `memory/shared/projects/iffy/cip-deployment-validation-report-2026-05-31.md` |
| Hero Frame Readiness | `memory/shared/projects/iffy/hero-frame-readiness-sprint-report-2026-05-31.md` |

---

## 3. VISUAL PRODUCTION OS v1 — CERTIFIED STATE

**Status:** ✅ COMPLETE ✅ CERTIFIED ✅ FROZEN
**Do NOT alter or redesign.** This layer is closed.

### Canonical Identity Chain (immutable)

```
Approved Corpus
  → Narrative Entity Layer (NEL)
  → Character Visual DNA
  → Character Identity Package (CIP) ← SINGLE CANONICAL VISUAL IDENTITY LAYER
      ├── Hero Frames    (generate-hero-frames)
      ├── Governance     (evaluate-visual-governance)
      ├── VPB            (vpb-assembly-engine)
      ├── Lookbook       (generate-lookbook-image)
      └── Poster         (generate-poster)
```

### 5/5 CANONICAL Consumers

| System | Identity Source | Fallback | Status |
|--------|:--------------:|:--------:|:------:|
| Hero Frames | CIP (`character_identity_packages`) | `character_visual_dna` | ✅ |
| Visual Governance | CIP status + DNA staleness | visual_sets (PD only) | ✅ |
| VPB Assembly | CIP | `character_visual_dna` | ✅ |
| Lookbook | CIP (face_traits, age_range, visual_descriptors, wardrobe_signals, appearance_constraints) | `character_visual_dna` | ✅ |
| Poster | CIP | `character_visual_dna` | ✅ |

### Ownership Boundaries

| Domain | Owner | Scope |
|--------|-------|-------|
| Approved Corpus | Architect / Morpheus | Source truth |
| NEL | Architect / Morpheus | Entity extraction |
| Visual DNA | Architect / Morpheus | Raw trait extraction |
| **CIP** | **Trinity** | **Canonical visual identity** |
| Hero Frames | Trinity / Kid | Character imagery |
| Governance | Trinity / Seraph | Readiness evaluation |
| VPB | Trinity | Visual production binder |
| Lookbook | Trinity / Keymaker | Visual reference |
| Poster | Trinity / Keymaker | Marketing poster |
| **Performance/Video OS** | **(NOT Visual Production)** | AI Actors, casting, performance |

### CIP Structure

- 4 asset classes: `character_production`, `casting_reference`, `actor_attachment`, `performance_reference`
- CIP fields: `face_traits`, `age_range`, `visual_descriptors`, `wardrobe_signals`, `appearance_constraints`
- Hero frames gate on `identityPackagesComplete`, NOT `castComplete`
- CIP = THE CHARACTER in story world, NOT THE ACTOR
- No automatic AI actor creation
- visual_sets retired from ALL identity paths (still exists for production-design context)

---

## 4. FEATURE FLAGS — ACTIVE

| Flag | Value | System | Set via |
|:----:|:-----:|--------|:-------:|
| `ENABLE_CIP_PIPELINE` | `true` | build-character-identity-package | Supabase env var |
| `ENABLE_CAST_SUGGESTIONS` | `true` | suggest-cast-from-dna | Supabase env var |
| `ENABLE_CAST_APPROVAL` | `false` | approve-cast-suggestion | Supabase env var (default) |

**Do NOT activate:** `ENABLE_CAST_APPROVAL` = false
**Do NOT begin:** AI Actor rollout
**Do NOT change:** CIP pipeline flags without validation

---

## 5. GOVERNANCE ASSUMPTIONS

### Visual Readiness vs Performance Readiness
- **Visual Readiness** = independent metric, separate from Performance Readiness
- Hero Frames gate on `identityPackagesComplete` → NOT `castComplete`
- Governance splits Visual Readiness evaluation from Performance Readiness
- evaluate-visual-governance checks: source_truth freshness, identity_packages completeness, hero_frames existence
- No actor dependency for visual readiness

### Governance Rules
- visual_sets used only as PD fallback (not identity)
- Stale detection: dependency-aware, timestamps removed from hash (was causing self-defeating stale)
- `SOURCE_SNAPSHOT_CHANGED` = informational severity only (not blocking)
- `isUsableVisualDNA` guard → filters strong-but-empty records

### Older Schema Projects
- Projects without `character_id` column (YETI-era) detected and skipped gracefully
- CIP pipeline creates records with null character_id, `asset_class=character_production`
- DNA fallback used for older projects that lack CIP records

---

## 6. KNOWN P2 DEFECTS

| # | Bug | System | Description | Priority |
|:-:|-----|:------:|-------------|:--------:|
| 1 | Production Draft resume + stale guard interaction | `generate-document/index.ts` | Stale guard (60 min) clears version before resume completes. Fix: extend stale guard threshold for resume contexts or add time-budget to chunk loop. | P2 |
| 2 | Character age drift in scene generation | Scene generation LLM | LLM generates inconsistent ages ("40s"/"30s" for same character). finalize-screenplay can't fix if canon_json `age_range` not populated. | P2 |
| 3 | `<center>` HTML tags in LLM output | Scene generation LLM | finalize-screenplay strips them deterministically, but underlying model issue persists. May recur with different models. | P2 |
| 4 | Scene-contract engine cold start after stale guard | `generate-document/index.ts` | Early resume check needs warm in-progress guard pass. Cold starts after stale guard clear lose contract data. | P2 |

**No P1 defects. No architecture defects. No CIP defects. No Governance defects.**

---

## 7. CURRENT PAUSE POINTS

1. **Visual Production OS v1 = FROZEN.** No further changes to CIP, Visual DNA, Governance, Hero Frames, VPB, Lookbook, or Poster architecture.
2. **AI Actor rollout = NOT STARTED.** `ENABLE_CAST_APPROVAL=false`. Cast suggestions exist but no AI actors created.
3. **v2 Storyboard Layer = NOT BUILT.** No storyboard_panels table, no generate-storyboard-from-cip function.
4. **v2 Visual Units = NOT BUILT.** No visual_units table.
5. **v2 Shot Planning = NOT BUILT.** No shot_plans table.
6. **AI Video Production = NOT STARTED.** Separate OS, external dependency.

---

## 8. NEW PRIMARY MISSION — PROJECT FACTORY CERTIFICATION

### Objective
Prove that IFFY can **repeatedly create projects** and move them through the full pipeline:
Development Engine → Approved Corpus → NEL → Visual DNA → CIP → Governance → Hero Frames → VPB → Export

Using the **actual product** — not simulations.

### Scope — ONLY TEST
1. **FEATURE FILM** ladder
2. **VERTICAL DRAMA** ladder

Do NOT expand into: Series, Documentary, Reality, Games, Animation, Other ladders

### Feature Film Test Set (6 projects)
1. Crime Thriller (Concrete Angels — already exists)
2. Horror / Creature (create new)
3. Sci-Fi Thriller (create new)
4. Period Drama (create new)
5. Romantic Comedy (create new)
6. Action Thriller (create new)

### Vertical Drama Test Set (6 projects)
1. Billionaire Romance (create new)
2. Revenge Drama (create new)
3. Mafia Romance (create new)
4. CEO Romance (create new)
5. Supernatural Romance (create new)
6. Family Melodrama (create new)

### Execution Strategy
- Run projects through the **REAL system**
- Do NOT fabricate data
- Do NOT bypass failures
- Do NOT manually patch records unless root-cause proves a system defect
- Use the platform exactly as a user would wherever possible

### Tracking Per Project

Track each stage:
- Development Generation Success
- Document Promotion Success
- Rewrite Success
- Approved Corpus Success
- NEL Success
- Visual DNA Success
- CIP Success
- Governance Success
- Hero Frame Success
- VPB Success
- Export Success
- Manual Interventions Required

Produce a **matrix** at the end.

### Certification Method
Every failure → EVIDENCE → DIAGNOSIS → PATCH PLAN → VALIDATION → DEFINITION OF DONE

Fix the defect. Re-run. Continue until ladder passes.

### Hard Rule
Prefer: wiring fixes, pipeline fixes, promotion fixes, cache fixes, orchestration fixes, governance fixes, generation fixes

Over introducing new systems. Do NOT build new architecture unless blocker absolutely requires it.

### Success Condition
Final answer classification:
- **A)** Factory Certified
- **B)** Certified with Minor Gaps
- **C)** Partially Certified
- **D)** Not Certified

Question to answer: *"Can Feature Film projects and Vertical Drama projects be reliably created and moved through Development Engine → Visual Production → VPB Export with minimal or zero manual intervention?"*

---

## 9. EXISTING TEST PROJECTS

### Concrete Angels (Crime Thriller)
- Feature Film ladder: COMPLETE through Production Draft (206K chars)
- Visual DNA: 4 characters, isUsableVisualDNA=true
- CIP: 4 records, character_production asset_class
- Hero Frames: generated, characters_actor_bound=0
- VPB: 14 sections, 5 assets
- Best candidate for re-run certification

### YETI (Horror/Creature)
- Feature Film ladder: partial (66 hero frames)
- 19 character_sets (older schema, no character_id)
- CIP: 19 records created with null character_id
- 20 character-bound frames, 40 environment frames
- Good compatibility test for older schema

### [TEST] Vert Drama
- Vertical drama test project
- Shadow-mode validated: no regressions
- Schema compatibility confirmed

---

## 10. KEY ARCHITECTURE DECISIONS (immutable)

1. CIP = single canonical visual identity layer — 5/5 consumers confirmed
2. Visual Production OS ≠ Performance/Video OS — separated formally
3. CIP represents THE CHARACTER in story world, not THE ACTOR
4. 4-class asset taxonomy: character_production | casting_reference | actor_attachment | performance_reference
5. Hero Frames gate on identityPackagesComplete, NOT castComplete
6. Cast suggestions auto-created at suggested only — no automatic AI actors
7. AI actors created ONLY via explicit approval (approve-cast-suggestion)
8. VPB excludes casting_reference and actor_attachment assets
9. Feature flags default OFF → shadow-mode validation → ON
10. visual_sets retired from identity paths (remains in PD context)

---

## 11. BUILD & DEPLOY COMMANDS

```bash
# Build
cd /Users/laralane/code/iffy
npm run build

# Deploy edge functions
npx supabase functions deploy <function-name> --project-ref hdfderbphdobomkdjypc

# Deploy all functions
npx supabase functions deploy --project-ref hdfderbphdobomkdjypc

# Run migrations
npx supabase migration up --project-ref hdfderbphdobomkdjypc

# Deploy to Vercel production
TOKEN=$(python3 -c "import json; print(json.load(open('/Users/laralane/Library/Application Support/com.vercel.cli/auth.json'))['token'])")
vercel deploy --prod --yes --token=$TOKEN

# Git
git add -A
git commit -m "fix(area): what was changed — why"
git push
# IMPORTANT: Always commit and push before kanban_complete
```

---

## 12. TEST PROJECT CREATION TEMPLATE

```sql
-- Create a new project via Supabase REST
INSERT INTO projects (title, format, genres, logline, premise, budget, tone)
VALUES ('Project Title', 'feature_film', '["genre"]', 'Logline...', 'Premise...', 'medium', 'dark')
RETURNING id;

-- Run ladder pipeline
-- Each stage: generate-document(doc_type) → promote → next
```

---

## 13. CRITICAL PITFALLS TO AVOID

1. **Uncommitted code is lost code** — always commit+before before kanban_complete
2. **Stale local git** — `git fetch origin && git log HEAD..origin/main --oneline` before inspecting code
3. **visual_sets is NOT identity** — never use it as primary identity source
4. **CIP is NOT actor data** — never mix character_production with actor_attachment
5. **No new architecture unless blocker requires it** — prefer wiring/pipeline fixes
6. **Feature flags default OFF** — never flip without shadow-mode validation
7. **Older schema projects (no character_id)** — detect and skip gracefully, use DNA fallback
8. **DUAL-WRITE hard rule** — all shared files go to BOTH machines: local + scp to 192.168.0.11
9. **Dual-write paths:** `~/.openclaw/workspace/memory/shared/communications/events/inbox/*.md`, `~/.openclaw/workspace/memory/shared/decisions/*.md`, `~/.openclaw/workspace/memory/shared/projects/iffy/*.md`, `~/agent-workspaces/architect/input/*.md`
10. **Sebastian responds to "Lls"** — it's a signal to load session continuity via session_search(), not a greeting

---

## 14. SESSION MEMORY STATE

### Current user profile entries
- Sebastian uses "Lls" as session_search() signal
- Supabase password: M33k0Drag0n, email: sebastianstreet@gmail.com
- Architecture-strict, retrieval-first, minimal patches
- TRINIUM format: STATUS/TASK/PROCESS — follow exactly
- P0 implementations MUST: feature flag OFF default, shadow mode, insertion point audit, fail-soft, telemetry only, rollback plan

### Current memory entries
1. Git fetch origin first on IFFY tasks — stale local code produces wrong reports
2. Supabase project ref: hdfderbphdobomkdjypc, token at ~/.config/supabase/access-token
3. CIP architecture decision — single canonical visual identity layer, two parallel tracks
4. File delivery rule: every file saved MUST be sent as Telegram file attachment via curl + bot token
5. Visual Production OS v1 closeout delivered 2026-05-31

---

## 15. IMMEDIATE NEXT STEPS (post-reset)

### Step 1 — Session Startup
```bash
git fetch origin && git log HEAD..origin/main --oneline  # check for stale local
python3 memory/trinity_memory.py --brief
python3 memory/jarvis_client.py status
python3 memory/jarvis_client.py search "recent decisions"
```

### Step 2 — Verify Current State
- Confirm HEAD = `555c563` (or newer from origin)
- Confirm working tree is clean
- Confirm Supabase project accessible
- Confirm feature flags in place

### Step 3 — Begin Project Factory Certification
1. Start with Concrete Angels (Crime Thriller) — full pipeline re-run from scratch
2. Create new projects for each genre in the test set
3. Track each stage systematically
4. Fix failures as they occur
5. Produce certification matrix

### Step 4 — Final Classification
Answer: "Can Feature Film projects and Vertical Drama projects be reliably created and moved through Development Engine → Visual Production → VPB Export with minimal or zero manual intervention?"

Classification: A/B/C/D

---

*End of NCP — prepared for immediate session reset.*
*All certified architecture state, feature flags, governance assumptions, P2 defects, pause points, validation reports, and the new mission are documented above.*